## LaTeX Quiz → PDF (16:9)

Project nằm trong thư mục `latex-quiz-pdf/`.

- **Chạy local**:

```bash
cd latex-quiz-pdf
npm install
npm run dev
```

- **Deploy Vercel**: khi import repo, đặt **Root Directory** = `latex-quiz-pdf`.

